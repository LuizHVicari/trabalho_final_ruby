<p style="color: green"><%= notice %></p>

<h1>Lists</h1>

<div id="lists">
  <% @lists.each do |list| %>
    <%= render list %>
    <p>
      <%= link_to "Show this list", [list.user, list] %>
    </p>
  <% end %>
</div>

<%= link_to "New list", new_user_list_path %>
